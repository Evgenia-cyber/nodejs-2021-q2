import { IUser } from '../users/user.types';

/**
 * All database users
 * @type {User[]}
 */
const USERS: IUser[] = [];

/**
 * All database boards
 *
 * @type {Board[]}
 */
const BOARDS: any = [];

/**
 * All database tasks
 *
 * @type {Task[]}
 */
const TASKS: any = [];

export { USERS, BOARDS, TASKS };
