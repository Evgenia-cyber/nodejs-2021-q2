/**
 * All database users
 * @type {User[]}
 */
const USERS = [];

/**
 * All database boards
 * @type {Board[]}
 */
const BOARDS = [];

/**
 * All database tasks
 * @type {Task[]}
 */
const TASKS = [];

module.exports = { USERS, BOARDS, TASKS };
